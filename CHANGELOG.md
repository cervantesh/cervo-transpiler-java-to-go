# Changelog

All notable changes to `cervo-transpiler-java-to-go` are tracked here.

## Unreleased

- Added enterprise governance documentation for install, scan, report, migrate, and review workflows.
- Added `cervo-migration.yaml` parsing and `j2go config validate`.
- Added deterministic migrate operation logs through `--log-file` / config.
- Added release workflow for Windows, Linux, and macOS binaries with SHA-256 checksums.

## 0.1.0 - Planned

- First tagged release candidate for the modern Java-to-Go migration CLI.
